<!-- Copyright 2026 Julien Bombled. Licensed under the Apache License, Version 2.0. -->
<UserControl x:Class="CortexCompanion.Views.HistoryView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:localization="clr-namespace:CortexCompanion.Localization" xmlns:models="clr-namespace:CortexCompanion.Models" xmlns:accessibility="clr-namespace:CortexCompanion.Accessibility">
    <UserControl.InputBindings>
        <KeyBinding Key="F5" Command="{Binding RefreshCommand}" />
    </UserControl.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <StackPanel>
            <TextBlock Text="{x:Static localization:UiStrings.HistoryNavigation}" TextWrapping="Wrap" Margin="{StaticResource MarginStackItem}" FontSize="{StaticResource FontSizeTitle}" />
            <TextBlock Text="{Binding Status}" TextWrapping="Wrap" Margin="{StaticResource MarginStackItem}" AutomationProperties.LiveSetting="Polite" accessibility:LiveRegionBehavior.Announcement="{Binding Status}" />
            <Button Content="{x:Static localization:UiStrings.HistoryRefresh}" AutomationProperties.Name="{x:Static localization:UiStrings.HistoryRefresh}" Command="{Binding RefreshCommand}" Style="{StaticResource PrimaryButtonStyle}" HorizontalAlignment="Left" Margin="{StaticResource MarginStackItem}" />
        </StackPanel>
        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <ItemsControl ItemsSource="{Binding Entries}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Style="{StaticResource PageCardStyle}" Margin="{StaticResource MarginSectionBottom}">
                            <StackPanel>
                                <TextBlock Text="{Binding Kind}" TextWrapping="Wrap" Margin="{StaticResource MarginStackItem}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding StartedText}" TextWrapping="Wrap" Margin="{StaticResource MarginStackItem}" />
                                <TextBlock Text="{Binding Status}" TextWrapping="Wrap" Margin="{StaticResource MarginStackItem}" FontWeight="SemiBold" />
                                <TextBlock Text="{Binding Counters}" TextWrapping="Wrap" Margin="{StaticResource MarginStackItem}" />
                                <TextBlock Text="{Binding Action}" TextWrapping="Wrap" Margin="{StaticResource MarginStackItem}" />
                                <Button Content="{x:Static localization:UiStrings.HistoryOpen}" AutomationProperties.Name="{x:Static localization:UiStrings.HistoryOpen}" Command="{Binding DataContext.NavigateCommand, RelativeSource={RelativeSource AncestorType=Window}}" CommandParameter="{Binding Destination}" Style="{StaticResource SecondaryButtonStyle}" HorizontalAlignment="Left" />
                                <Expander Header="{x:Static localization:UiStrings.HistoryDetails}" AutomationProperties.Name="{x:Static localization:UiStrings.HistoryDetails}" Margin="{StaticResource MarginSectionTop}">
                                    <TextBlock Text="{Binding Details}" TextWrapping="Wrap" Margin="{StaticResource MarginStackItem}" />
                                </Expander>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>